import { createContext } from "react";

const mainDataContext = createContext("");
const displayContext = createContext("");
const TMOContext = createContext("");
const projectModalControlContext = createContext("");

export {
  displayContext,
  mainDataContext,
  projectModalControlContext,
  TMOContext,
};
